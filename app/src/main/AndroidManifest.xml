<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.wit.pcgamelist">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".main.MainApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PcGameList">
        <activity android:name=".activities.DatabaseListActivity">
        </activity>
        <activity android:name=".activities.ReviewActivity">
        </activity>
        <activity android:name=".activities.ReviewListActivity">
        </activity>
        <activity android:name=".singlegamedetails.SingleGame">
        </activity>
        <activity android:name=".activities.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>